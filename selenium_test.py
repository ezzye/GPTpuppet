# filename: selenium_test.py
#
# from selenium import webdriver
# from selenium.webdriver.chrome.service import Service
#
# # Specify the absolute path to the downloaded ChromeDriver executable
# chrome_driver_path = '/usr/local/bin/chromedriver'  # Replace with the actual absolute path to the ChromeDriver exe
#
# # Initialize the Chrome driver with the specified path
# service = Service(executable_path=chrome_driver_path)
# driver = webdriver.Chrome(service=service)
#
# # Open Google's homepage to test if Selenium setup is correct
# driver.get("https://www.google.com")
# print("Google's homepage opened successfully.")
# driver.quit()